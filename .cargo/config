[alias]
bootloader-debug = "objcopy -- -O binary rboot-debug.bin"
bootloader-release = "objcopy --release -- -O binary rboot-release.bin"

[build]
target = "aarch64-thog-none.json"

[unstable]
build-std = ["core", "compiler_builtins"]
